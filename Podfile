# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Flow' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SnapKit', '~> 5.0.0'
  pod 'SwiftyBeaver'
  # Pods for Flow

  target 'FlowTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FlowUITests' do
    # Pods for testing
  end

end
